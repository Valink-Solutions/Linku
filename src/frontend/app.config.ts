export default defineAppConfig({
    meta: {
        title: 'Linku',
    },
    nuxtIcon: {
      size: '24px', // default <Icon> size applied
      aliases: {
        'nuxt': 'logos:nuxt-icon',
      }
    }
})